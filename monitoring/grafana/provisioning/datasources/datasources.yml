apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://cluster.local
    isDefault: true
    editable: false
    
  - name: Loki
    type: loki
    access: proxy
    url: http://cluster.local
    editable: false
